"""from .models import admin_login
from django.contrib.auth.models import User

class LoginBackend:
    def authenticate(self, username=None, password=None, model=None):
        if model == "Student":
            lookup_model = admin_login
        return user"""